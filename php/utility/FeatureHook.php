<?php
declare(strict_types=1);

// N4chan SDK utility: feature_hook

class N4chanFeatureHook
{
    public static function call(N4chanContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
